# EarthStep 開発タスク一覧

仕様書: [docs/spec.md](./spec.md)

本ドキュメントは仕様書を実装単位のタスクへ細分化したもの。
タスクIDは `フェーズ番号-連番` 形式。見積もりは1人日=8hの目安。

## 想定技術スタック(前提)

| 項目 | 選定 |
|---|---|
| 言語 | Kotlin |
| UI | Jetpack Compose |
| DI | Hilt |
| 非同期 | Coroutines / Flow |
| DB | Room |
| 設定保存 | DataStore (Preferences) |
| 位置情報 | Google Play Services Location (FusedLocationProvider) |
| 活動検知 | Play Services ActivityRecognition + Activity Transition API |
| 常駐 | Foreground Service (`foregroundServiceType="location"`) |
| 同期 | Google Drive REST API (AppDataFolder) + Credential Manager |
| minSdk / targetSdk | 26 / 最新 (Android 15+ の FGS 制限に対応) |

---

## フェーズ一覧とマイルストーン

| フェーズ | 目的 | 完了条件(Exit Criteria) |
|---|---|---|
| P0 プロジェクト基盤 | ビルドできる土台 | 空アプリがCIでビルド・Lint・テスト通過 |
| P1 データ層 | 永続化とマスタデータ | Room CRUD とマイルストーン100件がテストで検証済み |
| P2 計測エンジン | 距離を正しく測る中核 | 擬似位置列を入力した単体テストで距離計算が期待値と一致 |
| P3 常駐・権限 | バックグラウンドで動く | 端末をポケットに入れて30分歩き、距離が加算される |
| P4 ゲームロジック | XP・マイルストーン・周回 | 距離注入テストで100個の達成判定と周回繰り上げが通る |
| P5 UI/演出 | 遊べる画面 | ホーム・マップ・コレクション・達成演出・通知が動作 |
| P6 アセット | ドット絵・SE | バッジ20点+SE4種が組み込み済み |
| P7 バックアップ | 機種変更の担保 | エクスポート→別端末インポートで完全復元 |
| P8 品質保証 | 実地検証 | 実機での距離誤差とバッテリー消費が許容範囲 |
| P9 リリース | Play公開 | 内部テスト配信、審査提出物が揃う |

**MVP範囲**: P0〜P6 + P8。P7(クラウド同期)とP9は初回リリース直前でも可(ただしJSONエクスポートのみはMVPに含める)。

---

## P0. プロジェクト基盤 ✅ 実装済み

> 2026-08-20 追記: `dl.google.com` へ到達できる環境ではローカルでも
> `assembleDebug` / `testDebugUnitTest` / `ktlintCheck` / `detekt` / `lintDebug` がすべて通ることを確認済み。

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P0-1 | Androidプロジェクト作成 | Empty Compose Activity、パッケージ `com.example.earthstep`、Gradle Version Catalog (`libs.versions.toml`) 導入 | - | 0.5d |
| P0-2 | モジュール構成の決定 | 単一 `:app` で開始し、`core/data`・`core/domain`・`feature/*` のパッケージ分割で層を切る(マルチモジュール化は後回し) | P0-1 | 0.5d |
| P0-3 | Hilt導入 | Application クラス、`@HiltAndroidApp`、基本Module雛形 | P0-1 | 0.5d |
| P0-4 | 静的解析・整形 | ktlint / detekt、Android Lint の baseline 設定 | P0-1 | 0.5d |
| P0-5 | CI構築 | GitHub Actions: assembleDebug / lint / unit test。PRで必須チェック化 | P0-4 | 0.5d |
| P0-6 | ログ基盤 | Timber等。リリースビルドでは位置情報を出力しないポリシーを明文化 | P0-3 | 0.25d |
| P0-7 | テスト基盤 | JUnit5 or 4、Turbine、Robolectric、`kotlinx-coroutines-test` の導入と雛形テスト | P0-1 | 0.5d |

---

## P1. データ層 ✅ 実装済み (2026-08-20)

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P1-1 | Room導入 + Entity定義 | `daily_log` / `lifetime_stats` / `milestone_achievement` / `lap_record` を仕様6.1どおり定義 | P0-3 | 0.5d |
| P1-2 | DAO実装 | 日別距離のupsert(加算)、累計取得、達成一覧、周回記録。`exportSchema=true` でスキーマJSONをコミット | P1-1 | 1d |
| P1-3 | Migration方針 | schema/ ディレクトリ運用、`AutoMigration` 方針、マイグレーションテストの型を用意 | P1-2 | 0.5d |
| P1-4 | マイルストーンマスタ作成 | 仕様3.2の100件を静的データ化(`MilestoneCatalog`)。距離はm単位のLong、名称・カテゴリ・ランク・一言説明を保持 | - | 1d |
| P1-5 | マスタ検証テスト | 件数=100、距離が厳密昇順、100番目=40,075,000m、全件に名称が存在 | P1-4 | 0.25d |
| P1-6 | 大台フラグの付与 | 演出を分ける「大台」マイルストーンにフラグを立てる(現状 #100 のみ)。カテゴリ・ランクはバッジ廃止に伴い不要 | P1-4 | 0.25d |
| P1-7 | Repository層 | `ProgressRepository`(距離加算、Flowで累計/当日距離を公開)、`MilestoneRepository`、`SettingsRepository`(DataStore) | P1-2 | 1d |
| P1-8 | 日付境界の扱い | ローカルタイムゾーン基準の日付キー生成、日付跨ぎ・タイムゾーン変更時の挙動を単体テスト化 | P1-7 | 0.5d |

### P1 の実装メモ

| 決めたこと | 理由 |
|---|---|
| 日別距離の加算は `INSERT OR IGNORE` + `UPDATE ... SET distance_m = distance_m + ?` の2文 | SQLite の UPSERT (`ON CONFLICT ... DO UPDATE`) は 3.24 以降。minSdk 26 (Android 8.0 = SQLite 3.18) では使えない。2文をリポジトリのトランザクションで束ねる |
| `lifetime_stats` は `id` 固定の単一行 | 更新系をすべて UPDATE 文で書けるようにするため。行が無い場合は更新前に `INSERT OR IGNORE` で作る |
| 時刻・タイムゾーンは `AppTimeSource` 経由でのみ取得 | 日付境界をテスト可能にするため。タイムゾーンは実行中に変わるのでキャッシュしない |
| 当日距離の Flow は `DayTicker` で日付変更時に張り替え | 深夜0時に加えて最大1時間ごとに再確認し、タイムゾーン変更も拾う |
| マイルストーンの一言説明は `description: String?` を用意して全件 null | 文面が未執筆のため(下記 要確認事項1)。器だけ先に用意した |
| 達成の冪等化は PK + `OnConflictStrategy.IGNORE` | P4-3 の前倒し。DAO 側で二重記録が起きない状態にしておく |
| Room のスキーマJSONは `app/schemas/` へコミット | `SchemaExportTest` がバージョンとテーブル数を検証し、出し忘れをCIで落とす |

---

## P2. 計測エンジン(最重要・最難関) ✅ 実装済み (2026-08-20)

> 位置・センサーのAPIを直接触る層と、純粋な計算層を必ず分離する。
> 計算層(`DistanceAccumulator`)はAndroid非依存にし、擬似ログを流す単体テストで検証する。

実装の対応:

| 層 | 置き場所 | 中身 |
|---|---|---|
| 計算層(Android非依存) | `core/domain/measurement` | `Geo` / `DistanceAccumulator` / `StopDetector` / `RetroactiveDistanceBuffer` / `DailySoftCap` / `LocationIntervalPlanner` / `step/*` / `MeasurementProcessor` |
| API層 | `core/data/measurement` | `FusedLocationDataSource` / `StepCounterDataSource` / `PlayActivityRecognitionDataSource` / `MeasurementEngine` |

擬似走行ログは `app/src/test/resources/measurement/` にCSVで置き、`PseudoTrackDistanceTest` が期待距離と照合する。

### 2-A 距離計算コア(Android非依存)

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P2-1 | 位置サンプルのモデル定義 | `LocationSample(lat, lon, accuracy, speed, timestampMs, isMock)` と `StepSample` | - | 0.25d |
| P2-2 | 距離計算(Haversine) | 2点間距離、単体テストで既知距離と照合 | P2-1 | 0.25d |
| P2-3 | 精度フィルタ | accuracy > 30m の点を除外/重み下げ(仕様1.5) | P2-2 | 0.5d |
| P2-4 | 区間速度上限フィルタ | 前後2点の瞬間速度が 30〜40km/h 超の区間は加算しない | P2-3 | 0.5d |
| P2-5 | 3点整合性チェック | A→B→C で AB・BC が異常かつ AC が正常なら B を外れ値として除去し A→C で計算 | P2-4 | 0.75d |
| P2-6 | モック位置除外 | `isFromMockProvider()` の区間をXP対象外にし、ログへ記録 | P2-3 | 0.25d |
| P2-7 | 停止判定 | 速度<1.5km/h かつ歩数増加なしが2〜3秒継続で停止確定(仕様1.4) | P2-2 | 0.75d |
| P2-8 | 遡及除外バッファ | 直近2〜3秒分の加算距離をリングバッファ保持し、停止確定時に取り消す | P2-7 | 0.75d |
| P2-9 | 再開判定(ヒステリシス) | 徒歩速度域+新規歩数が2〜3秒継続で再開 | P2-7 | 0.5d |
| P2-10 | 日次ソフト上限 | 1日100kmを超えた分はXP加算を停止しログへ記録。日付変更でリセット | P1-8 | 0.5d |
| P2-11 | 距離計算の総合テスト | 実走行を模した擬似ログ(直線/カーブ/ジッター/GPSジャンプ/トンネル欠測/モック)をリソース化し、期待距離との誤差を検証 | P2-1〜P2-10 | 1.5d |

### 2-B 歩数フォールバック

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P2-12 | 歩数センサー購読 | `TYPE_STEP_COUNTER` を常時購読。非搭載端末のフォールバック分岐も用意 | P2-1 | 0.5d |
| P2-13 | 再起動リセット補正 | 前回値との差分が負ならベースラインを補正(仕様1.3)。永続化必須 | P2-12 | 0.75d |
| P2-14 | 歩幅の自己較正 | GPS有効区間で `歩幅 = GPS距離 ÷ 歩数` を移動平均。異常値(30〜100cm外)は棄却し `lifetime_stats.stride_length_cm` へ保存。初期値は既定70cm | P2-12 | 1d |
| P2-15 | フォールバック切替判定 | 位置更新が15〜20秒途絶 or accuracy>50m で歩数モードへ。短時間瞬断で往復しないヒステリシスを実装 | P2-3 | 0.75d |
| P2-16 | 切替時の起点記録 | 切替の瞬間に歩数カウンタ値とGPS距離の起点を記録し、二重カウント・欠落を防止 | P2-15 | 0.5d |
| P2-17 | フォールバックのテスト | GPS欠測→歩数→GPS復帰のシナリオで距離が二重加算/欠落しないことを検証 | P2-12〜P2-16 | 0.75d |

### 2-C Android API 連携

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P2-18 | ActivityRecognition購読 | `ON_FOOT` 信頼度50%以上をXP対象。`ON_BICYCLE`/`IN_VEHICLE` は対象外(仕様1.1) | P0-3 | 1d |
| P2-19 | Activity Transition API | `STILL`→`ON_FOOT` の遷移でGPSを起動、`ON_FOOT`→`STILL` でGPSを停止 | P2-18 | 1d |
| P2-20 | 位置更新の可変間隔 | 移動中は `PRIORITY_BALANCED_POWER_ACCURACY` で5〜10秒。直線が続けば10秒、方向転換が多ければ5秒へ寄せる適応ロジック | P2-19 | 1d |
| P2-21 | エンジン統合 | 上記入力を `MeasurementEngine` に集約し、確定距離を Repository へ流す | P2-11, P2-17, P2-20 | 1d |

### P2 の実装メモ

| 決めたこと | 理由 |
|---|---|
| 棄却した区間では起点(アンカー)を進めない | これだけで仕様1.5の3点整合性チェック(A→B→C で B が外れ値なら A→C)が特別扱い無しに成立する。低精度点を挟んでも直線の距離が失われない |
| 加算した距離は3秒寝かせてから確定させる | 停止確定時の遡及除外(仕様1.4)を「DBへ書いた距離を後から引く」処理無しで実現するため。停止が確定したら窓の中の距離を捨てるだけでよい |
| 起点の保持は20秒まで | それを超える欠測は歩数フォールバックの担当。GPSで直線を引くとトンネル区間が二重計上になる |
| 距離を作るのは常にGPS・歩数のどちらか一方だけ | モードが変わる瞬間に両方の起点を捨てることで、切り替え点をまたぐ区間がどちらでも加算されない(P2-16) |
| 距離はm未満の端数を持ち越して整数で永続化。計測終了時のみ四捨五入 | 5秒ごとの加算で丸め誤差が積もらないようにするため |
| 活動判定は `ON_BICYCLE` / `IN_VEHICLE` を明示的に除外し、`STILL` / `UNKNOWN` では判定を変えない | 静止は速度と歩数を見る停止判定(仕様1.4)の方が確実なため |
| 計測エンジンは入力を1本の `MeasurementInput` 列へ束ねる | 位置・歩数・活動判定が非同期に届くため、個別に状態を持つと到着順に依存したバグが出る |
| ActivityRecognition のレシーバは動的登録 | 計測していない間に起こされても捨てるだけなので、マニフェストへ静的登録すると無駄に電力を使う |

> マニフェストには `ACCESS_FINE/COARSE_LOCATION` と `ACTIVITY_RECOGNITION` のみ追加した。
> 背景位置・フォアグラウンドサービス・通知の各権限は P3-2 で追加する。
> 実際に権限をリクエストする導線(P3-3)がまだ無いため、この時点では実機で計測は動かない。

---

## P3. 常駐サービス・権限・オンボーディング ✅ 実装済み (2026-08-20)

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P3-1 | Foreground Service実装 | `foregroundServiceType="location"`、常駐通知(現在の累計距離/次のマイルストーンまで)を表示 | P2-21 | 1d |
| P3-2 | マニフェスト権限宣言 | `ACCESS_FINE/COARSE_LOCATION`、`ACCESS_BACKGROUND_LOCATION`、`ACTIVITY_RECOGNITION`、`POST_NOTIFICATIONS`、`FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_LOCATION`、`RECEIVE_BOOT_COMPLETED` | P3-1 | 0.25d |
| P3-3 | 権限リクエストフロー | 仕様7.1の順序で段階リクエスト。背景位置は理由説明画面→設定画面誘導 | P3-2 | 1d |
| P3-4 | Android 13+ 通知権限 | `POST_NOTIFICATIONS` のリクエストと拒否時の縮退動作 | P3-3 | 0.25d |
| P3-5 | 再起動後の自動復帰 | `BOOT_COMPLETED` 受信でサービス再開。歩数ベースライン補正(P2-13)と連携 | P3-1 | 0.5d |
| P3-6 | プロセス死からの復帰 | サービス再生成時の状態復元、`START_STICKY` 方針、未確定距離の永続化 | P3-1 | 0.75d |
| P3-7 | バッテリー最適化除外導線 | チュートリアル完了時に1画面挟み `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` を発行。拒否時も動作継続 | P3-3 | 0.5d |
| P3-8 | オンボーディング画面 | 3〜4枚(ゲームルール説明 / 位置情報の理由 / 背景位置の理由 / 通知とバッテリー)。各権限リクエストの直前に配置 | P3-3 | 1.5d |
| P3-9 | 権限縮退時のUI | 権限が欠けている状態をホームで常時警告表示し、再取得導線を出す | P3-3 | 0.5d |

### P3 の実装メモ

| 決めたこと | 理由 |
|---|---|
| 版数ごとの必要権限は `PermissionRequirements.requiredOn(sdkInt)` に集約 | SDK 版数を引数に取る純粋関数にすることで、実機やエミュレータ無しで各バージョンの分岐をテストできる |
| 背景位置は Android 11 以降だけ設定画面へ誘導 | Android 10 はシステムダイアログで直接聞ける。版数で分岐せずに常に設定画面へ送ると、Android 10 で余計な手間が増える |
| オンボーディングは全ページをスキップ可能にした | 権限を拒否しても縮退して動く設計のため。拒否された状態はホームの警告(P3-9)で常時提示する |
| 常駐通知は 10m 刻みでのみ更新 | 1m ごとに更新すると歩いている間じゅう通知を書き換えることになる |
| サービス停止は `stopService` を使う | 停止用の intent を `startService` で投げる方法は Android 8 以降のバックグラウンド起動制限にかかりうる。`stopService` なら制限を受けず、`onDestroy` 経由で寝かせていた距離も書き切られる |
| 計測開始前に必ず権限を確認する | Android 14 以降、位置情報の権限が無い状態で location 型の FGS を開始すると `SecurityException` で落ちる |
| ホームの警告は「位置情報が無い」ときに背景位置の警告を重ねない | まず「測れない」を直してもらう。同時に何行も出しても読まれない |

**P3-6 の制約**: 確定した距離は都度DBへ書いているため、プロセスを強制終了されても失われるのは
遡及除外バッファに入っている最大3秒ぶん(徒歩なら数メートル)に限られる。この範囲は
「二重計上を避けるために確定を遅らせている」ぶんなので、永続化せず破棄する方が安全と判断した。
歩数センサーの基準値と歩幅は DataStore / DB に永続化済みで、サービス再生成時に読み直す。

**未検証**: 実機・エミュレータでの動作確認は行っていない。常駐の実挙動、権限ダイアログの遷移、
再起動後の復帰は P8(品質保証・実機検証)で確認する。

---

## P4. ゲームロジック ✅ 実装済み (2026-08-20)

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P4-1 | XP換算 | 1m = 1XP。累計距離(m)をそのままXPとして扱うユースケース | P1-7 | 0.25d |
| P4-2 | マイルストーン達成判定 | 距離更新のたびに未達成の該当分を検出。**1回の更新で複数達成**するケースを必ず考慮 | P1-4, P4-1 | 0.75d |
| P4-3 | 達成の冪等化 | 同一マイルストーンの二重記録を防ぐ(DBのPK+トランザクション) | P4-2 | 0.5d |
| P4-4 | 次のマイルストーン算出 | 次の対象と残り距離、進捗率をFlowで公開 | P4-2 | 0.5d |
| P4-5 | 周回ロジック | 40,075km到達で `current_lap` を+1し `lap_record` を記録。1回の更新で2周以上進む場合も処理 | P4-2 | 0.75d |
| P4-6 | 周内マーカー | 25%(10,019km) / 50%(20,038km) / 75%(30,056km) の到達検出(仕様4.2) | P4-5 | 0.5d |
| P4-7 | 2周目以降の表示切替 | 2周目以降は100マイルストーンを再提示せず、周回カウンター+周内マーカーのみ | P4-5 | 0.5d |
| P4-8 | 周回スキン切替 | 1・2・3・5・10・20・50・100周目でトレイル色/地球儀スキンを変更、以降100周ごと | P4-5 | 0.5d |
| P4-9 | ゲームロジック単体テスト | 距離を段階注入し、達成順・複数同時達成・周回繰り上げ・マーカー発火を検証 | P4-1〜P4-8 | 1d |

### P4 の実装メモ

| 決めたこと | 理由 |
|---|---|
| 距離が増える経路を `RecordDistanceUseCase` の1本にまとめた | 計測エンジンが `ProgressRepository.addDistance` を直接呼ぶと、達成判定を通らない距離が生まれる |
| マイルストーンは「範囲」ではなく「累計に届いていて未記録のもの」で判定 | プロセスが落ちて判定を1回飛ばしても、次の更新でまとめて拾える。1回の更新で複数達成(P4-2)も自然に処理できる |
| 周回数は累計距離から毎回計算し直す | カウンタを+1する持ち方だと、取りこぼしや二重処理で累計距離と周回数がずれたまま戻らなくなる。累計距離を唯一の真実にすれば何度計算しても同じ答えになる |
| 周内マーカーは永続化しない | 演出のための通知だけが目的。取りこぼしても次の周で出る |
| 進捗率は「直前のマイルストーンから次まで」の区間で測る | 累計距離を次の距離で割ると、序盤の間隔が狭い区間でバーがほとんど動かないように見える |
| マイルストーン一覧の表示可否は「未達成が残っているか」で判定 | 100個目の到達と1周の走破が同じ距離なので結果は同じだが、達成状況を直接見る方が意図が読める(P4-7) |
| 周回スキンは色を使い切ったら先頭へ戻す | 素材は8種類。色が足りないことでクラッシュさせるより、見た目が一巡する方が害が小さい(仕様4.3 の「余力に応じて増減可」) |
| 地球一周の距離を `Earth.CIRCUMFERENCE_METERS` へ一本化 | マイルストーン#100 と周回の1周が同じ値。2か所に持つとどちらかだけ直す事故が起きる |

**周内マーカーの距離**: 仕様4.2 は約10,019 / 20,038 / 30,056km と丸めた値だが、
実装は 40,075km の厳密な 1/4・1/2・3/4(10,018.75 / 20,037.5 / 30,056.25km)を使う。

---

## P5. UI・演出 ✅ 実装済み (2026-08-20)

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P5-1 | デザイントークン | ドット絵向けカラーパレット、Press Start 2P 等のドットフォント、9-patch風のドットUI部品 | - | 1d |
| P5-2 | ピクセル描画の基盤 | `FilterQuality.None` / `isFilterBitmap=false` によるニアレストネイバー拡大の共通Composable(仕様5.4の実装注意) | P5-1 | 0.5d |
| P5-3 | ホーム画面 | 累計距離・XP・当日距離・周回数・次のマイルストーンまでの距離・進捗バー | P4-4 | 1.5d |
| P5-4 | 2Dドットマップ | 正距円筒図法のドット世界地図。**赤道上を東へ進む単純方式**(周内進捗÷40,075kmをX座標へ写像、Yは赤道固定)。右端→左端へラップ | P5-2, P4-1 | 2.5d |
| P5-5 | トレイル描画 | 階段状ドットライン(滑らかな曲線にしない)。周回ごとの色バリエーション適用 | P5-4, P4-8 | 1d |
| P5-6 | 達成記録画面 | 100件のリスト。未到達はグレー表示、タップで詳細(番号/名称/距離/説明/到達日)。バッジ画像は持たない | P1-4 | 1d |
| P5-7 | 達成演出:通常 | 達成記録に項目が加わる軽いスケールアニメーション+効果音 | P5-6, P6-2 | 0.75d |
| P5-9 | 達成演出:大台 | フルスクリーン演出、マップのカメラワーク、四角ドットのパーティクル、実在物の一言説明。#100と周内マーカーで使い回す1種 | P5-7 | 1.5d |
| P5-10 | 演出内の情報表示 | 名称/累計距離/次までの距離(仕様3.4) | P5-7 | 0.5d |
| P5-11 | 周内マーカー演出 | 25%(赤道→北極点の豆知識)/50%(対蹠地到達)/75%(数字のみ)。P5-9の大台演出を流用 | P4-6, P5-9 | 0.75d |
| P5-12 | 達成通知 | 「🎉[名称]に到達」通知、タップで演出画面へ。複数同時は「まとめて3個達成」に集約 | P4-2, P3-4 | 1d |
| P5-13 | 演出キュー | バックグラウンドで貯まった未再生の演出を、次回起動時に順番に再生する仕組み | P5-12 | 0.75d |
| P5-14 | 設定画面 | 権限状態、バッテリー最適化、エクスポート/インポート、同期、歩幅の較正値表示、リセット | P3-9 | 1d |
| P5-15 | ナビゲーション | Navigation Compose によるホーム/マップ/コレクション/設定の遷移、通知からのディープリンク | P5-3 | 0.5d |

### P5 の実装メモ

| 決めたこと | 理由 |
|---|---|
| 寸法をすべて `PixelDimens.Unit`(4dp)の整数倍にした | 1ドットの大きさが揃わないと、枠線の太さや余白がばらついて素材が浮いて見える |
| `PixelCanvas` を通してドット座標で描く | Canvas は連続座標で描けるため、素直に描くとドット境界が小数に落ちてアンチエイリアスでにじむ |
| 進捗バーはドットで刻む | 滑らかに伸びる帯はドット絵の画面から浮く。「あと何ドット」の見え方の方がゲームらしい |
| 達成記録はタップで同じ行を開く | 別画面へ飛ばすと、100件のどこを見ていたか見失いやすい |
| 未到達のマイルストーンも名称を伏せない | 次に何を目指すのか分からないと距離を積む動機にならない |
| 演出はどの画面にも重ねる | 貯まっている演出があれば必ず目に入るようにするため(P5-13) |
| 演出キューは DataStore に置き、上限20件で打ち切る | 失っても「演出を見逃す」だけ。長く起動しないまま貯まりすぎると、起動時に延々と見せられる方が困る |
| 通知は複数同時達成をまとめる | 歩いている間に何度も鳴るのは、達成の嬉しさより煩わしさが勝つ |
| 出来事の受け渡しは `RecordDistanceUseCase` から `ProgressEventSink` へ | 通知も演出も「距離を記録した結果」。呼び出し側に回すと渡し忘れる余地が生まれる |

**P6 待ちの箇所**(いずれも動作はする。素材が入れば見た目が上がる):

| 箇所 | 現状 | 差し替え先 |
|---|---|---|
| ドットフォント | 等幅フォントで代用。差し替え点は `pixelFontFamily` の1か所 | P6-1 相当(Press Start 2P は日本語を持たないため、数字と見出しのみ適用する想定) |
| ドット世界地図 | 72x36 の手書き陸地マスク(`WorldMapDots`)。海岸線は正確ではない | P6-3 |
| 効果音 | **未実装**。通常/大台とも音は鳴らない | P6-2 |
| タブのアイコン | Material の既定アイコン | P6-4 |

**未実装**: エクスポート/インポートと同期(P7)。設定画面には未実装である旨のみ表示している。
実機・エミュレータでの表示確認は行っていない(P8-7 の Compose UIテストで担保する)。

---

## P6. アセット制作

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P6-2 | 効果音 | フリー素材(効果音ラボ等)から2〜3種選定(通常/大台/UI操作)。ライセンス表記をアプリ内クレジットへ | - | 0.5d |
| P6-3 | ドット世界地図 | 正距円筒図法のドット地図画像(横方向にシームレスに繋がること) | P5-4 | 1.5d |
| P6-4 | UI部品素材 | ボタン、プログレスバー、枠、アイコン類のドット素材 | P5-1 | 1d |
| P6-5 | アプリアイコン/スプラッシュ | アダプティブアイコン、Play用フィーチャーグラフィック | P6-4 | 0.5d |
| P6-6 | ライセンス管理 | フォント(Press Start 2P: OFL)・効果音・地図データの出典とライセンスを `LICENSES` に集約 | P6-2 | 0.25d |

---

## P7. バックアップ・同期 (P7-1〜P7-5 ✅ 実装済み 2026-08-20)

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P7-1 | JSONシリアライズ | 仕様6.3の形式(`schema_version`, `exported_at`, 各テーブル)で入出力するモデル | P1-2 | 0.5d |
| P7-2 | 手動エクスポート | SAF(`ACTION_CREATE_DOCUMENT`)でJSONを書き出し | P7-1 | 0.5d |
| P7-3 | 手動インポート | SAF で読み込み、`schema_version` を見て変換。バージョン不一致時の挙動を定義 | P7-1 | 0.75d |
| P7-4 | 競合解決ロジック | `daily_log` は日付キーで max、達成/周回は和集合(達成日時は早い方)、`total_distance_m` は日別ログから再計算(仕様6.4) | P7-3 | 1d |
| P7-5 | 競合解決テスト | 2端末分のJSONを合成し、二重カウントが起きないこと・過小評価の範囲を検証 | P7-4 | 0.5d |
| P7-6 | Googleサインイン | Credential Manager + Google ID による認証 | P0-3 | 1d |
| P7-7 | Drive AppDataFolder連携 | Drive REST API でJSON1ファイルをアップロード/ダウンロード | P7-6, P7-1 | 1.5d |
| P7-8 | 自動同期スケジュール | WorkManager で定期同期(1日1回+手動トリガ)。オフライン時のリトライ | P7-7 | 0.75d |
| P7-9 | 同期状態UI | 最終同期日時、失敗理由、手動同期ボタン | P7-8, P5-14 | 0.5d |

### P7-1〜P7-5 の実装メモ

| 決めたこと | 理由 |
|---|---|
| JSONの読み込みは寛容に、書き出しは厳密に | 手で編集されたJSONや将来フィールドが増えたJSONを読めなくして記録を丸ごと失わせるより、読める範囲で受け入れる方が損失が小さい |
| `schema_version` は既定値でも必ず書き出す | 省略されると読み込む側が形式の版数を判定できない。`encodeDefaults = true` で強制する |
| 新しい版数のJSONは読み込まずに知らせる | 内容を推測して取り込むと壊れた状態になりうる。版数の判定を最初に行い、部分的な取り込みを防ぐ |
| インポートは上書きではなく必ずマージ | 機種変更でも同期でも「両方の端末で歩いた分を残す」のが目的 |
| 累計距離は日別ログの合計から再計算 | 端末ごとの累計値を信じると、片方が壊れていたときに引きずられる |
| 周回数は再計算した累計から導く | P4 と同じく累計距離を唯一の真実にする |
| DBへの書き戻しは差分適用でなく総入れ替え | マージの結果は「あるべき全体像」。部分的に足すと、マージで負けた側のレコードが残る |
| 歩幅はより多く歩いた側の値を採る | 歩幅はGPS区間の実績から少しずつ較正されるため(P2-14)、歩いた距離が長い側の方が本人の歩幅に寄っている |
| 読めない日時は 0 として扱う | 日時が1件おかしいだけでインポート全体を失敗させるより、距離を守る方を優先する |

**仕様6.3 との差分**: `milestone_achievements` に `distance_m_at_achievement` を追加した。
仕様の例では省略されているが、DBスキーマ(仕様6.1)が持つ列であり、
書き出さないと機種変更で失われる。読み込み時に無ければ、そのマイルストーンの閾値距離で埋める。

**過小評価の範囲(P7-5 で検証)**: 同じ日に2台で別々に歩いた場合、その日は多い方だけが残る。
実測 5,000m と 4,000m なら合計 9,000m ではなく 5,000m になる。
二重計上は一度起きると取り消せず、マイルストーンも周回も先に進んでしまうため、
「少なく数える」側へ倒している(仕様6.4 のトレードオフどおり)。

**未実装**: P7-6〜P7-9(Googleサインインと Drive 同期)。
Google Cloud のクライアントID発行が必要なため未着手。設定画面には未実装である旨を表示している。

---

## P8. 品質保証・実機検証

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P8-1 | 距離精度の実地検証 | 既知距離(400mトラック等)を複数回歩き、誤差率を計測。目標を数値で定義(例: ±5%以内) | P3-1 | 1d |
| P8-2 | 屋内フォールバック検証 | 地下街・ビル内で歩数モードへ切替わるか、切替前後で距離が飛ばないか | P2-17, P3-1 | 0.5d |
| P8-3 | 誤検知検証 | 自転車・電車・自動車でXPが加算されないことを確認 | P2-18 | 0.75d |
| P8-4 | バッテリー消費計測 | Battery Historian で終日稼働の消費を計測。許容値を定義(例: 1日5%以内) | P3-1 | 1d |
| P8-5 | 長時間安定性 | 24〜72時間連続稼働でのプロセス死・メモリリーク・サービス復帰を確認 | P3-6 | 1d |
| P8-6 | 端末マトリクス検証 | Android 13/14/15+、歩数センサー非搭載端末、複数メーカー | P3-5 | 1d |
| P8-7 | UIテスト ✅ 実装済み (2026-08-20) | Compose UIテストで主要画面と演出フローを自動化 | P5-15 | 1d |
| P8-8 | Crash計測 | Crashlytics 等の導入(位置情報を送らない設定) | P0-6 | 0.5d |

### P8-7 の実装メモ

Compose の UI テストは `androidTest` ではなく **Robolectric 上の単体テスト**として置いた。
`androidTest` は実機・エミュレータが必要で CI では実行されず、壊れても気づけないため。
実機での描画確認は P8-6(端末マトリクス検証)の担当。

| テスト | 対象 |
|---|---|
| `HomeScreenTest` | 累計距離・XP・当日距離・周回数・次のマイルストーン・権限警告(P5-3 / P3-9) |
| `MapScreenTest` | 周回数・周内進捗・次の周内マーカーまでの残り(P5-4) |
| `CollectionScreenTest` | 100件の一覧、未到達表示、タップでの詳細開閉(P5-6) |
| `CelebrationOverlayTest` | 通常/大台の演出、25/50/75%の文言、演出内の情報表示(P5-7 / P5-9 〜 P5-11) |
| `CelebrationFlowTest` | 貯まった演出を起きた順に再生し、閉じ切ると消える(P5-13) |
| `SettingsScreenTest` | 権限状態、エクスポート/インポート、リセットの確認ダイアログ(P5-14 / P7-2 / P7-3) |
| `OnboardingScreenTest` | 枚数・説明の直後にリクエスト・スキップ・完了(P3-8) |

**UIテストで見つけた問題**: ホーム画面に到達不能な分岐があった。
100個目のマイルストーンの距離と1周の距離は同じ(仕様3.1)ため、
「一覧を出すが次の目標が無い」状態は起こらない。分岐を削除した。
あわせて、リセットの確認ダイアログのボタンが一覧側と同じ文言だったので、
どちらを押したか読み取れるよう別の文言(「消す」)に分けた。

**未着手**: ナビゲーション(P5-15 のタブ遷移)は、各画面が Hilt 経由で ViewModel を取るため
UIテストから直接組み立てられない。タブの遷移そのものは薄いので、
必要になれば `HiltAndroidTest` を足して `androidTest` 側で見る。

---

## P9. リリース準備

| ID | タスク | 内容 | 依存 | 見積 |
|---|---|---|---|---|
| P9-1 | 署名・難読化 | Release署名鍵、R8設定、Play App Signing | P0-5 | 0.5d |
| P9-2 | プライバシーポリシー | 位置情報の用途・端末内保存・Driveバックアップを明記したページを公開 | - | 0.5d |
| P9-3 | 背景位置の審査対応 | Google Play のバックグラウンド位置情報宣言フォーム、用途説明動画の準備(仕様7.1-6) | P9-2 | 1d |
| P9-4 | ストア掲載物 | スクリーンショット、説明文、フィーチャーグラフィック | P6-5 | 0.5d |
| P9-5 | 内部テスト配信 | Play Console 内部テストトラックへ配信、フィードバック収集 | P9-1 | 0.5d |
| P9-6 | データ安全性フォーム | Play の Data Safety セクション記入 | P9-2 | 0.25d |

---

## 見積サマリ

| フェーズ | 概算 |
|---|---|
| P0 プロジェクト基盤 | 約 3.3d |
| P1 データ層 | 約 5.3d |
| P2 計測エンジン | 約 15d |
| P3 常駐・権限 | 約 6.3d |
| P4 ゲームロジック | 約 5.3d |
| P5 UI・演出 | 約 14.5d |
| P6 アセット | 約 3.8d |
| P7 バックアップ | 約 7d |
| P8 品質保証 | 約 6.8d |
| P9 リリース | 約 3.3d |
| **合計** | **約 70人日** |

実装以外(実地検証の待ち時間、Play審査、アセットの手戻り)を含めると、個人開発で3〜4ヶ月規模。
バッジ廃止により約4人日を削減した。

---

## クリティカルパス

```
P0-1 → P1-1 → P1-7 → P2-1..P2-11(距離計算コア) → P2-21 → P3-1(常駐)
                                                    ↓
                                        P8-1/P8-4(実地検証)
```

P5(UI)とP6(アセット)は P4 完成後に並行可能。P7 は独立性が高く後回しにできる。
**P2の距離計算コアが最大のリスク**なので、ここを最優先で着手し、擬似ログテストで早期に精度を確定させる。

---

## 要確認事項(仕様書からは決まらない点)

実装前に判断が必要な項目。

### 決定済み (2026-08-19)

- ~~バッジのランク境界~~ → **バッジ・ランクとも廃止**。報酬は「到達演出」と「達成記録への追加」の2層のみ
- ~~地図上の現在地の決め方~~ → **赤道上を東へ進む単純方式**を採用

### 未決 (実装をブロックしない順)

1. **一言説明文(大台演出用)**: 仕様3.4の「実在物についての一言説明」は未執筆。P5-9 の実装までに、少なくとも大台ぶんが必要。**ユーザー保留中**。
   実装側は `Milestone.description`(nullable)を用意済みで、文面が決まり次第 `MilestoneCatalog` へ入れるだけでよい。
2. **「大台」マイルストーンの選定**: 現状は #100 と周内マーカーのみ。#100 だけだと大台演出が生涯1回になるため、区切りの良い数件(例: #23 マラソン、#91 地球の直径)を追加する案がある。
   P1-6 は「#100 のみ `isMajor`」で実装済み。追加する場合は `MilestoneCatalog` のフラグと `MilestoneCatalogTest` を更新する(P5-9 の実装までに決めればよい)。
3. **アプリのパッケージ名/表示名**: `io.github.task320.earthstep` を仮採用(`com.example.*` は Play へ公開できないため)。独自ドメインがあれば P9-1 までに差し替え。
4. **最低サポートAndroidバージョン**: minSdk 26 を仮置き。Activity Transition API と歩数センサーの普及状況からは妥当だが、要確認。
5. **バッテリー最適化除外の Play 審査**: `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` は Google Play の
   制限付き権限で、用途の申告が必要。フィットネス系の常時計測は一般に認められる用途だが、
   通らなかった場合は `ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS`(一覧画面への誘導、権限不要)へ
   切り替える。実装済みのフォールバックがそのまま使える。P9-3 で対応する。
6. **オフライン地図の権利**: ドット地図を自作するか、パブリックドメインの世界地図(Natural Earth 等)からドット化するか。
7. **97番〜98番の間隔**: 仕様3.1の宿題(22,117km〜35,000km を埋める実在距離)。未解決のままでも実装は進められる。
